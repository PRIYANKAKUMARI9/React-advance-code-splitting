import './App.css';
import Aboutt from './Aboutt'

import('./mathhh').then((result)=>{
  console.warn("result",result.addd(10,202));
})

function App() {
  return (
    <div className="App">
    hello
   <Aboutt/>
    </div>
  );
}

export default App;
